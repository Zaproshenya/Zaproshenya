{
  "name": "zaproshennya-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for the Запрошення app — handles cross-user notifications, stats aggregation, and orphan-data cleanup.",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "engines": { "node": "18" },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
